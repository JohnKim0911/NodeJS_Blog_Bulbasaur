<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles/styles.css">
    <title>Bulbasaur's Grove</title>
</head>

<body>
    <header>
        <a href="/">
            <h1>🍃 Bulbasaur's Grove 🍃</h1>
            <p>Where Cuteness Meets Adventure!</p>
        </a>
    </header>

    <nav>
        <a href="/" <% if (currentPage == 'home') { %>class="active" <% } %> >Home</a>
        <a href="/about" <% if (currentPage == 'about') { %>class="active" <% } %> >About</a>
        <a href="/album" <% if (currentPage == 'album') { %>class="active" <% } %> >Album</a>
    </nav>
